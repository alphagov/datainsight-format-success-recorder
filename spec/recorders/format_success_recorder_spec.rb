require_relative "../spec_helper"
require_relative "../../lib/recorders/format_success"

describe "FormatSuccessRecorder" do


  before(:each) do
    @message = {}
  end

  after(:each) do

  end

  it "should store valid message"

  it "should update existing measurements"

  describe "validation" do
    it "should raise an error if model is invalid"

    it "should fail if value is not present"

    it "should fail if value cannot be parsed as a integer"

    it "should not allow nil as a value"
  end
end
